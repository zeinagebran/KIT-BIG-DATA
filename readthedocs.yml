version: 2
build:
  os: ubuntu-20.04
  tools:
    python: "3.9"
python:
  install:
    - requirements: requirements.txt
sphinx:
  configuration: docs/source/conf.py
hooks:
  post-build:
    - cp docs/index.html _build/index.html
